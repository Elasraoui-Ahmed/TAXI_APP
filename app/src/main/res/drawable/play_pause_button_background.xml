<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When button is pressed -->
    <item android:drawable="@drawable/button_pressed_background" android:state_pressed="true"/>

    <!-- When button is not pressed (normal state) -->
    <item android:drawable="@drawable/button_normal_background"/>
</selector>
